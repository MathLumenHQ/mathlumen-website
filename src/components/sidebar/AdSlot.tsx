/**
 * Future ad placement — enable at 2000+ monthly visitors.
 * Hidden by default. Replace `display: none` with actual ad unit when ready.
 */
export function AdSlot() {
  return (
    <div style={{ display: "none" }} aria-hidden="true">
      {/* Ad unit goes here */}
    </div>
  );
}
